= form.render_form do |f|
  = form.render_errors(f)

  .form__field
    = f.label :message
    = f.text_field :message, autofocus: true
  .form__field
    = f.label :algorithm
    = f.select :algorithm, Ping::VALID_ALGORITHMS
  .form__actions
    = f.submit 'Save', class: 'button button--blank'
